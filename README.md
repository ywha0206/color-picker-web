# 색상 선택도구 🎨

React로 구현된 인터랙티브 색상 선택 도구입니다. 다양한 색상 형식(RGB, HEX, CMYK, HSV, HSL)을 지원하며, 실시간으로 색상을 선택하고 변환할 수 있습니다.

## 주요 기능

- 🎨 **인터랙티브 색상 팔레트**: 마우스 클릭으로 색상 선택
- 🌈 **색조 슬라이더**: 색조를 조정하여 다양한 색상 범위 탐색
- 🔍 **실시간 미리보기**: 선택한 색상을 즉시 확인
- 📊 **다양한 색상 형식**: RGB, HEX, CMYK, HSV, HSL 형식으로 색상 표시
- 📱 **반응형 디자인**: 모바일과 데스크톱에서 모두 사용 가능
- 🎯 **직접 HEX 입력**: HEX 코드를 직접 입력하여 색상 설정

## 프로젝트 구조

```
src/
├── components/
│   ├── ColorPicker.js          # 메인 색상 선택기 컴포넌트
│   ├── ColorPalette.js         # 색상 팔레트 (캔버스)
│   ├── ColorPreview.js         # 선택된 색상 미리보기
│   ├── ColorCodeDisplay.js     # 색상 코드 표시
│   └── HueSlider.js           # 색조 슬라이더
├── utils/
│   └── colorUtils.js          # 색상 변환 유틸리티 함수
├── styles/
│   ├── App.css               # 메인 앱 스타일
│   └── index.css             # 전역 스타일
├── App.js                    # 메인 앱 컴포넌트
└── index.js                  # React 앱 진입점
```

## 설치 및 실행

### 1. 의존성 설치
```bash
npm install
```

### 2. 개발 서버 실행
```bash
npm start
```

브라우저에서 `http://localhost:3000`으로 접속하여 앱을 확인할 수 있습니다.

### 3. 프로덕션 빌드
```bash
npm run build
```

## 사용법

1. **색상 선택**: 팔레트 영역을 클릭하여 원하는 색상을 선택합니다.
2. **색조 조정**: 슬라이더를 드래그하여 색조를 변경합니다.
3. **HEX 입력**: HEX 코드를 직접 입력하여 정확한 색상을 설정합니다.
4. **색상 정보 확인**: 선택한 색상의 RGB, CMYK, HSV, HSL 값을 확인합니다.

## 기술 스택

- **React 18**: 사용자 인터페이스 구축
- **HTML5 Canvas**: 색상 팔레트 렌더링
- **CSS3**: 스타일링 및 반응형 디자인
- **JavaScript ES6+**: 색상 변환 로직

## 색상 변환 지원

- **RGB ↔ HEX**: 16진수 색상 코드 변환
- **RGB → CMYK**: 인쇄용 색상 형식
- **RGB → HSV**: 색상, 채도, 명도 형식
- **RGB → HSL**: 색상, 채도, 밝기 형식

## 브라우저 지원

- Chrome (최신 버전)
- Firefox (최신 버전)
- Safari (최신 버전)
- Edge (최신 버전)

## 라이선스

MIT License
